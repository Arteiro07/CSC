Houve varias dificuldadesa realizar este projeto o que nos levou a entregar este esqueleto de projeto.

O Admin Funciona a 100%

O client cria as queries e encripta-as homomorficamente, mas não as assina ou encripta

O server cria uma base de dados e um comparador de bits, mas não executa o resto das funcionalidades.

Para compilar basta usar o make em cada pasta 

para correr é só rrer ./execs/run
